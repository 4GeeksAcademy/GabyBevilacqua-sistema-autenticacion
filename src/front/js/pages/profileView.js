import React, { useContext } from "react";
import { Profile } from "../component/profile";

export const ProfileView = () => {

    return (

        <div>
            < Profile />
        </div>
    )
};